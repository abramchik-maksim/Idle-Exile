# Design Changelog

## 2026-04-01

- Split monolithic `Assets/_Game/GAME_DESIGN.md` into modular docs under `Assets/_Game/Design/`.
- Added index-style `Assets/_Game/GAME_DESIGN.md` with read order and scope contract.
- Added finalized `Gain As` and `Penetration` model in `Assets/_Game/Design/Combat.md`.
- Marked separation between `Current implementation` and `Target design` across new docs.

## 2026-04-01 (Migration Pass 2 - No Data Loss)

- Kept `Assets/_Game/GAME_DESIGN.md` untouched as legacy baseline.
- Expanded modular docs with full legacy tables, examples, formulas, and UI behavior text.
- Kept and extended `Gain As` and `Penetration` sections in `Assets/_Game/Design/Combat.md`.
- Added explicit `Legacy Baseline` and `Current Implementation (Code-Backed)` structure to each major module.
